# Dataset

The original project notebook references a dataset named:

`XPREP8.csv`

The dataset is not currently included in this repository.

The dataset was used by the original analysis for:
- Financial data exploration
- Technical indicator analysis
- Classification of the `worked?` target
- Regression of `percentage_returns given`

## Dataset Status

The source dataset is currently unavailable in this repository.

Therefore, `Analysis.ipynb` cannot be executed end-to-end without obtaining the original `XPREP8.csv` file.

## Reproducibility

The notebook and project report are retained in the repository as the project record. The dataset can be added later if it is available and suitable for redistribution.
